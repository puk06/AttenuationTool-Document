---
sidebar_position: 1
---

# 音声制御無効化コライダーが上手く動かない...

これはコライダーの優先順位の問題で、入っているコライダーの決定時に、音声制御無効化コライダーよりも上に防音室コライダーがあると機能しなくなります。

## 解決手順

1. `Attenuation Tool` コンポーネントを開く
2. `コライダーオブジェクト（個別）` の欄を開く
3. `音声制御無効化ゾーン` の入ったコライダーを一番上に配置する
